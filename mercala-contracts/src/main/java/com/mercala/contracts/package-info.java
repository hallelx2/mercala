/**
 * Mercala shared contracts: event records (e.g. {@code OrderPlaced}, {@code ProductAdded})
 * and cross-service DTOs. Kept dependency-light so every service can depend on it.
 */
package com.mercala.contracts;
